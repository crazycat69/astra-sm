#! /bin/sh

version=$(git describe --match "v[0-9]*" --abbrev=7 HEAD)

version=`echo -n $version | sed \
    -e 's/^v//' \
    -e 's/-\([0-9]*\)-g.*/.\1/' \
    -e 's/-g\(.*\)/-\1/'`

if [ "$version" != "" ]; then
    echo -n $version
else
    echo -n "UNKNOWN"
    exit 1
fi

exit 0
